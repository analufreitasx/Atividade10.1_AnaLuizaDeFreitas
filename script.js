function Multiplicação (a, b){
    return a * b;
}

const num1 = Number(prompt('Digite um número inteiro: '))
const num2 = Number(prompt('Digite outro número inteiro: '))
alert(" O produto entre " + num1 + " e " + num2 + " é igual a " + Multiplicação(num1,num2) + ".")